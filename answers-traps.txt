Q1: Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf?
A1: a0-a7 contain arguments to function. Register a2 holds 13 in main's call to print.

Q2: Where is the call to function f in the assembly code for main? Where is the call to g? 
A2: Function f and g all be inlined by compiler.

Q3: At what address is the function printf located?
A3: 0x64a

Q4: What value is in the register ra just after the jalr to printf in main?
A4: ra = pc(0x34) +4 = 0x38

Q5: What is the output? If the RISC-V were instead big-endian what would you set i to in order to yield the same output? 
    Would you need to change 57616 to a different value?
A5: Output is HE110 World. I would set i to 0x726c6400. There is no need to change 57616 to a different value.

Q6: In the following code, what is going to be printed after 'y='? Why does this happen?
A6: It will print value in register a2. Because the second arguments is not provided.

